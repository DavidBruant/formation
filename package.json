{
  "name": "formation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "tweets": "node generateTweets.js",
    "build": "browserify src/vanilla/main.js -o bundle.js -t [ babelify ] -d --extension=jsx",
    "watch": "watchify src/vanilla/main.js -o bundle.js -t [ babelify ] -d -v --extension=jsx",
    "build:react": "browserify src/react/main.jsx -o bundle.js -t [ babelify ] -d --extension=jsx",
    "watch:react": "watchify src/react/main.jsx -o bundle.js -t [ babelify ] -d -v --extension=jsx",
    "build:redux": "browserify src/redux/main.jsx -o bundle.js -t [ babelify ] -d --extension=jsx",
    "watch:redux": "watchify src/redux/main.jsx -o bundle.js -t [ babelify ] -d -v --extension=jsx"
  },
  "jest": {
    "setupFiles": [
      "./jest.setup.js"
    ]
  },
  "lint-staged": {
    "*.js": [
      "eslint",
      "jest --findRelatedTests"
    ]
  },
  "huksy": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.1.2",
    "@babel/preset-env": "^7.1.0",
    "@babel/preset-react": "^7.0.0",
    "babel-core": "^7.0.0-bridge.0",
    "babelify": "^10.0.0",
    "enzyme": "^3.3.0",
    "enzyme-adapter-react-16": "^1.1.1",
    "husky": "^1.1.2",
    "isomorphic-fetch": "^2.2.1",
    "jest": "^23.6.0",
    "lint-staged": "^7.1.0"
  },
  "dependencies": {
    "moment": "^2.22.1",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "twit": "^2.2.11"
  }
}
